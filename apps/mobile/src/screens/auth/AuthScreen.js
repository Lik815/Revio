import React from 'react';
import { ScaffoldScreen } from '../_shared/ScaffoldScreen';

export function AuthScreen() {
  return (
    <ScaffoldScreen
      title="Anmeldung"
      description="Hier wird im nächsten Schritt der bisherige Login-, Register- und Invite-Flow aus dem Legacy-App-Objekt in eigenständige Screens überführt."
    />
  );
}
